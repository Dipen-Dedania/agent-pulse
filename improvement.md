# Improvement list

- Settings screen should have back button so user can properly navigate back & forth
- Can we detect IDE installed on the user machine based on the operating system? We would like to disable it in the setting screen. Also try to show installation location in the card if possible.
- Setting page Toggle switch should have cursor pointer. Same goes for all the buttons.
- Let's create States Reference of agents as a separate component in the code.
- We have duplicate assets in the 2 different folders. "assets" & "public/assets". Let's have only single copy. Double check the usage reference.
- We have logo.png & favicon folders in the assets. Let's use both to make our app like production grade. Follow apple design system. Also, let's use size that don't break in macos or windows in all the cases.
- Let's add troubleshoot steps for end user in case sometimes hooks doesn't work. E.g. in case of gemini CLI, we have to restart the CLI.
- Bubble shadow gets cut while animating. Let's reduce the bubble size to 75%.
